<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AplikasiFormIdentitasMahasiswa" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryDark">@color/primary_blue</item>
        <item name="colorAccent">@color/primary_blue</item>

        <!-- Control colors -->
        <item name="colorControlNormal">#BDBDBD</item>
        <item name="colorControlActivated">@color/primary_blue</item>
        <item name="colorControlHighlight">@color/primary_blue</item>
    </style>
</resources>