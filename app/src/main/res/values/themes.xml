<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AplikasiFormIdentitasMahasiswa" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorAccent">@color/primary_blue</item>
        <item name="android:colorControlActivated">@color/primary_blue</item>
    </style>

    <style name="Theme.AplikasiFormIdentitasMahasiswa" parent="Base.Theme.AplikasiFormIdentitasMahasiswa" />
</resources>